import styled from 'styled-components';

const NoteWrap = styled.div `
    background-color: rgba(255,255,255,0.5);
`

const Note = () => {
    return(
            <>
            <NoteWrap>
            Note
            </NoteWrap>
                
            </>
    )
}

export default Note;